// Opérations Mathématiques;
// % = modulo -> reste de la division euclidienne
// ** = exponentielle/puissance -> 5**2 = 25
let nombreUn = 8,
    nombreDeux = 4,
    result; // on peut déclarer plusieurs variables sur la même ligne
result = (nombreUn + nombreDeux) * 9; //
result++; // incrémentation de + 1
result--; // décrémentation de - 1
console.log(result);
